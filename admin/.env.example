VITE_API_URL=http://localhost:5001

# Base URL of the main client app — admin's "Forgot password?" link deep-links
# there rather than duplicating the reset-password UI in this app too, since
# password reset is shared across both apps' logins (same users table).
VITE_CLIENT_URL=http://localhost:5173
